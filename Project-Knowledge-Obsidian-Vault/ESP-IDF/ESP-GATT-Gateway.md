# ESP GATT Gateway (ESP-IDF)

Main file:

- `ESP/ESP_GATT_BLE_Gateway/main/main.c`

Read these functions first:

1. `process_gatt_command()` at `:563`
2. `send_vendor_command()` at `:353`
3. `custom_model_cb()` at `:403`
4. `gatt_notify_sensor_data()` at `:222`

Then read [[Components/ESP-GATT-Gateway-Map]].
